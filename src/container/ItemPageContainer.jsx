import { AspectRatio,Box,Center,HStack,Image,Text} from 'native-base';

export const ItemPageContainer = ({title}) => {
  return (

       
        <Text fontSize={24} fontWeight="bold">
       
          {title}
       
        </Text>
  );
};
